{{#modal-tools maxWidth='320px' logoTitle=true}}

	<form {{action "signIn" on="submit"}}>
		{{#if loading}}
			<div class="field">
				<p>Loading...</p>
			</div>
		{{else}}
			<div class="field">
				<label for="install_field_email">What is your <strong>email address</strong>?</label>
				{{input type="email" id="install_field_email" value=email placeholder="Email Address" required=true}}
			</div>

			<div class="field">
				<label for="install_field_password">What is your <strong>password</strong>?</label>
				{{input type="password" id="install_field_password" value=password placeholder="Password" required=true}}
			</div>
		{{/if}}
		
		<div class="modal_actions">
			<button type="submit" class="clickable button">
				Sign In To Remetric
			</button>
		</div>
	</form>
	
	{{#link-to 'forgot' class='bottom_link'}}
		Forgot your password?
	{{/link-to}}

{{/modal-tools}}