import {
  eachInObject
} from 'remetric/helpers/each-in-object';

module('EachInObjectHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = eachInObject(42);
  ok(result);
});
