import {
  storyTeller
} from 'remetric/helpers/story-teller';

module('StoryTellerHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = storyTeller(42);
  ok(result);
});
